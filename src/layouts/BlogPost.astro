---
import Base from './Base.astro'
const props = Astro.props.content
const {title} = props
---

<Base {...props}>
  <article class="home-section blogPost">
    <h1>{title}</h1>
    <slot>Default Blog Content</slot>
  </article>
</Base>